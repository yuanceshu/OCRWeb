# Project Agent Notes

- 默认不要把移动端适配作为本项目当前任务的验收项或阻塞项；用户会在单独任务中处理移动端。
- 除非用户明确要求移动端、390px、响应式断点或手机截图，本项目的前端验证优先覆盖桌面与当前任务相关交互。
- 不要因为移动端视觉问题扩大当前任务范围；记录问题即可，继续完成用户当前要求。
- PC 端当前表现已定版；后续默认所有修改都只针对手机端 UI。调整手机端时必须限制在移动端断点、移动端专用覆盖层或明确的手机端逻辑内，不允许导致 PC 端格式、布局、样式、素材结构或交互表现发生变化；每次手机端修改都必须回归桌面 `1600×900` live 页面确认无回退。
- 当前网页状态以用户真实浏览器打开的 `http://localhost:5173/` 和当前源码为准；不要把 `.codex-screens/` 或 `交付/` 里的历史截图直接当作当前页面。
- 做视觉对比时，先确认 dev server 正在跑，并使用当前 live 页面截图。桌面视觉验证优先使用接近用户浏览器的宽视口，例如 1600×900；不要用默认 1280×720 视口代表最终桌面效果。
- 本项目的历史截图和交付图很多，命名中含 `current`、`final`、`target`、`v3`、`no-gray` 等也可能只是某一轮过程产物。引用这些图片前要说明它们是“历史截图/目标图/交付图”，不要混淆为 live 页面。
- `项目回收站/` 是项目内的本机回收站，只用于暂存旧素材、历史截图、中间生成物和迁移清理文件；不要把其中内容当作当前源码、当前视觉依据或交付来源。
- 当前项目已进入“浏览器兼容性检查 + 代码/资产清理”阶段；除非用户明确回到功能开发或视觉改版，不要再把“继续扩完整接口内容”当作默认下一步。
- 产品/设计方向相关任务先读 `PROJECT_VISION.md` 和 `DESIGN.md`：前者记录产品定位和体验原则，后者记录当前视觉系统和实现规则。
- 本项目是拟物档案风格界面，纸面、容器、布面、卡片等关键材质必须优先使用生图/位图材质或已有素材实现；不要用纯 CSS 渐变背景冒充真实纸张或材质。若要消除素材里的细线/脏纹，应优先生成或替换更干净的材质图，而不是覆盖一层低质渐变。
- 首页“索引目录”左侧纸卡禁止直接使用带黑灰规整内框线的 `paper-card-alpha.png`；应使用已清理内框线的专用素材或同等干净纸卡素材，保留自然纸纹和粗糙纸边，不要重引入四边机械框线。
- 对于像首页“接口图鉴”这种高度随内容连续变化的大纸面容器，禁止再用 `border-image ... fill` 或整张 `cover` 大纸图去承担中心纸面；应采用“边缘位图单独处理 + 中心无缝纸纹平铺”的结构，避免暗色内板、拉伸糊感和方向性纹理回潮。
- 首页“接口图鉴”大纸面禁止再叠加 `home-record-paper-alpha.png` 里那圈灰色圆角矩形内框线；这条线会在动态容器里重复成机械分割线。自然纸纹、粗糙撕边、折痕、污渍和磨损需要保留。
- 首页“接口图鉴”大纸面的外壳如果来自同一张完整纸面素材，禁止再拆成互不连续的顶部条和左右条去硬拼角部；外壳必须保持同源连续，中心纸底再独立做无缝平铺。
- 首页“接口图鉴”大纸面的左右粗糙边缘不能压在上下边缘之上，也不能从上边缘上方开始露出方块贴片；上下边缘应作为角落收口层覆盖左右边缘。
- 首页“接口图鉴”卡片在桌面端不要再使用“整卡任意区域点击即跳转”的交互；摘要、字段标签、分类名、记录号等正文区应允许直接选中复制，跳转只应落在标题、缩略图、底部动作区这类明确热点上。
- 首页首屏纸面标题区不要再使用无功能的顶部标签（如 `CATALOGUE`、`OCR-ATLAS`、`No. 0001`）；若删除后空间显空，应通过标题组版式、真实印章或 OCR 档案语义元素平衡，而不是恢复无意义标签。
- 首页首屏搜索框不要做成现代 SaaS 式大白框；应像纸质档案检索条或索引条，使用纸纹、旧纸边、短阴影和克制圆角。
- 首页首屏右侧不要再使用淡灰椭圆水印章；如使用章类装饰，当前接受方向是参考用户截图的浅灰透明罗盘印痕：细线、低透明、旧纸压印感、无底片矩形/圆斑，不要回到粗重蓝色徽章或普通水印。
- 首页首屏右侧四张样例卡片的“已收录”标记不要再退回纯 CSS 简化圆章；应保持位图印章质感，像旧纸面上低透明压印的收录章，而不是细描边蓝色圆圈。
- 首页左侧装订件若保留，默认方向应贴近 `首页参考图.png`：细装订环穿过小孔眼、线材感轻、结构克制；不要再回到厚黄铜、粗横杆、工业五金件式的笨重装订结构。
- 对纯图 Banner 的生图审稿，不要只把纸张和布面材质做得更真；字体气质、标题组版、导航字重、搜索图标、按钮图标和小型装饰符号也必须体现更强的档案/编目设计感，避免停留在“材质变好了，但字和图标还是普通 UI”的层级。
- 对首页小 logo、标题签、角标、章标这类小尺寸生图资产，禁止走“纪念品徽章 / 廉价复古贴图 / 黄铜小零件拼装”路线；小尺寸资产应优先保证档案系统语义、克制的材料语言和 46px 左右场景下的清晰识别，不要为了“复古”堆满边饰、齿轮、金属圈、勋章边和无意义装饰。
- 个人身份类卡片预览图允许共享同一类档案桌面场景，但证件本体必须优先接近真实证件的常见版式、信息密度、标题结构和材质记忆点；不要生成“通用信息卡 / 银行卡式卡片 / 演示 UI 卡片”来冒充身份证、通行证、社保卡、驾驶证等证件。
- 个人身份类证件预览图必须使用虚构或遮挡信息、无效号码、样张水印等方式避免可用证件感，但不能因此破坏真实证件的核心视觉结构。身份证尤其不能缺失国徽页的关键标题结构，也不能把正反面排成过度留白的浅蓝白通用卡。
- 个人身份类预览图优先复用首页 Banner 小卡片的成功方式：先生成或绘制独立的证件样本，再合成到档案纸卡/桌面场景中；中文字段、号码、地址、机关、期限等可读文字优先本地排版，避免让生图模型在整张场景图里直接生成中文导致字段漂移或不可读。
- `idcard-thumb-v2` 到 `idcard-thumb-v7` 这批新生成身份证缩略图已被用户否定，当前应回退并保持使用旧版 `idcard-thumb.jpg`。其他个人身份类证件缩略图也默认保持已有旧版 `*-thumb.jpg`，除非用户明确要求重新生成某一个证件。
- 当前运行时代码应引用同名 `*.webp` 缩略图；上述 `*.jpg` 作为已认可基准素材保留，不应误删或重新接入运行时。
- 生成首页 Banner 或接口样张类 OCR 材料资产时，优先参考 `docs/ocr-specimen-prompt.md` 的提示词预案：用高保真材料本体位图承载真实版式记忆、字段密度和清晰文字，再用示例/无效/SAMPLE 语义消除可用证件风险。该流程是未来 OCR 样张生成 skill 的预备版本。
- 若 OCR 样张生成触发证件/票据安全合规拒绝，不要尝试绕过或提高真实感；应按 `docs/ocr-specimen-prompt.md` 的“安全合规降级策略”处理：先生成不可用材料外壳，再本地叠加虚构字段、全 0/全 X 号码、无效日期和 SAMPLE/示例标记。
- 字段来源示意图可优先使用 CSS 构造的合规示意版本：不追求和真实证件/票据完全一致，但字段位置、照片/图章区域和文字内容必须清晰可对照真实材料版式；信息必须明显虚构、无效，并带有“示例/样张/仅供字段来源示意”等语义，避免可用证件感。这类字段来源示意资产不受 `frontend-design` 技能的界面风格约束。
- 当前详情页“字段来源示意”方向已下线；上面这条只在用户明确重启该方向时才重新生效。
- 开通流程指引页必须坚持“流程优先”原则：用户核心问题是“怎么申请流程”，默认只展示流程相关内容，不要塞无关卡片、泛化概览或旁支信息导致阅读路径分散。
- 开通流程指引页顶部导航必须与首页导航保持同构和同风格，不允许进入子页面后换成另一套菜单结构造成迷路感。
- 开通流程指引页导航层级必须清晰区分：流程层级与步骤层级使用不同视觉样式；左侧流程索引仅列真实主流程（当前为两条），不要把示例区或附件说明伪装成并列主流程。
- 开通流程指引页不要提供“原始 Word 下载”作为首要动作；附件下载入口只在对应流程步骤中按需出现（例如“添加申请表附件”步骤）。
- 接口详情页必须坚持“接入关键信息优先”原则：用户进入证件/票据接口正文页时，第一眼应能看到测试地址、生产地址、请求参数、返回参数这几个高频任务入口，并且这些入口应高亮、可点击、能直达；不要只在右侧放一个速查卡后仍让正文主线分散。
- 接口详情页的信息架构应把“调用地址 / 请求参数 / 返回参数”作为主阅读路径；字段来源、样张、场景、相关接口等能力理解内容可以保留，但不能抢占首屏和导航的最高优先级。
- 接口详情页在首屏已完整展示调用地址时，不要再在正文重复设置独立“调用地址”章节；避免主线重复和页面拉长。
- 接口详情页左侧导航必须用清晰分组区分主接入路径与辅助内容；不要把“字段来源”“错误码”等辅助章节做成视觉上像“返回参数”子菜单的样子。
- 接口详情页左侧导航如需二级导航，默认只允许 `返回参数` 使用，并优先采用“进入返回参数区域后展开、离开后收起”的方式控制高度；不要把所有二级项长期摊开占满整张吸顶导航卡。
- 接口详情页左侧导航的分组标题、分隔线和导航条目必须共享一致的内边距基线；不要让“辅助理解”“示例参考”等标题贴着纸卡边缘，造成压迫感和层级误读。
- 接口详情页左侧导航中，“主路径入口的常驻重点样式”和“当前激活项样式”必须明确区分；不要让 `请求参数`、`返回参数` 这类主路径按钮长期看起来像已选中状态，造成当前章节判断混淆。
- 接口详情页左侧导航中的 `返回参数` 一级项在被点击时必须立即展开二级小节，不能只依赖滚动监听延迟触发展开。
- 未获用户明确要求前，不要在任何子页面重新加入“字段来源示意”模块，也不要在左侧索引中恢复对应入口。
- 与用户迭代视觉、结构或交互时，凡用户明确表示“不喜欢 / 不想要 / 需要调整 / 不要再出现”的内容，必须记下来：项目级长期规则写入 `AGENTS.md`，当前轮次的具体反例和禁忌同步写入 `SESSION_PROGRESS.md`，避免后续页面重复犯错。
- 如果页面视觉看起来退回旧版，优先检查视口宽度、资源是否完全加载、浏览器缓存和 CSS 断点，而不是立刻假设源码丢失。
- 新 Mac 上如系统 `npm` 不可用，本项目可先使用 `.tools/node`：运行项目前将 `PATH="$PWD/.tools/node/bin:$PATH"` 放到命令前或导出到当前终端。
- Git 依赖 Xcode Command Line Tools；如果 `git` 报 `xcode-select` 相关错误，先让用户完成 `xcode-select --install` 的系统安装流程，再做提交、分支或状态判断。
- 独立交付页 `交付/OCR模型自训练全流程操作指南.html` 使用用户明确要求的新视觉方向：深石墨背景、浅色文字、信号橙重点色。禁止复用蓝色档案布板、浅色纸张、旧纹理/装饰素材和旧页面样式；Word 中真实的平台操作截图仅作为教学内容保留。流程梗概固定为首屏等权六步，平台详细操作独立放在下方。
