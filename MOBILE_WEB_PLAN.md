# OCRWeb 手机端任务规划与进度跟踪

> Last updated: 2026-06-05
> Purpose: 手机端专项的任务拆解、验收标准和 session 交接指针。每次继续手机端任务时，先读 `SESSION_PROGRESS.md`，再读本文件。

## 当前结论

手机端不能按“桌面端缩小版”处理。当前桌面端信息架构基本成立，手机端需要重新组织阅读顺序、导航方式和交互模型。

当前手机端基线来自 live 页面 `http://localhost:5173/`，诊断视口为 `390x844`：

- 首页：无横向溢出，但首屏效率偏低。统一纸质导航约占 207px，接口卡列表约在 1277px 后才开始出现，用户要滚过标题区和完整索引目录才能看到主要内容。
- 接口详情页：存在 P0 破版。390px 下页面被撑到约 835px，`.detail-hero` 和 `.section-block` 约 804px 宽，首屏只能看到左侧导航和正文左半边。
- 开通流程页：无横向溢出，方向基本可用，但首屏导航和标题区偏重，流程索引与首屏流程卡存在一定重复。

当前手机端专项状态：

```text
M0 现状诊断与规划：已完成
M1 接口详情页手机端可用性修复：已完成
M2 全站手机端导航与纸面密度：已完成
M3 首页手机端结构重排：已完成
M4 字段表 / 代码块移动交互：已完成
M5 开通流程页手机端优化：已完成
M6 验证、回归与文档收口：已完成
M7 手机端成品化体验优化：首轮完成，待用户视觉验收
```

## M7 手机端成品化体验优化

目标：在不扰乱 PC 端定版视觉的前提下，把手机端从“工程上可用”推进到“手机用户一眼知道怎么完成任务”。

### M7 第一性原则

1. PC 端冻结优先。
   - 当前 PC 端视觉和布局视为冻结基线。
   - 手机端改动默认只进入 `max-width: 720px` / `max-width: 480px` 的移动端覆盖层。
   - 不改 `:root`、全局字体、桌面网格、桌面素材结构和 PC 首屏布局，除非用户明确要求。

2. 手机端任务优先。
   - 首页优先让用户搜索、筛选、进入接口。
   - 详情页优先让用户判断接口、复制地址、进入请求参数和返回参数。
   - 开通流程页优先让用户判断走哪条流程，以及第一步做什么。

3. 档案风保留，但装饰降权。
   - 保留纸张、布面、样张和档案气质。
   - 手机端减少大纸面、大样张、大留白和重复索引的占高。
   - 不把手机端改成普通 SaaS 白卡片或通用文档列表。

4. 每期必须可验证。
   - 每期至少验证 `390x844`、`428x932` 和桌面 `1600x900`。
   - 所有验收视口必须满足 `scrollWidth === clientWidth`。
   - 桌面截图不允许出现顶部导航、首页首屏、详情页左侧导航、流程页桌面索引的明显回退。

### M7 框线图

首页手机端：

```text
┌─────────────────────────┐
│ OCR 识别接口文档中心      │
│ 首页  全部接口  开通流程   │
│ [联系支持]               │
├─────────────────────────┤
│ OCR 接口图鉴              │
│ 接口换种读法...           │
│ [ 搜接口名 / 字段 / 类别 ] │
├─────────────────────────┤
│ 类型筛选  → 横向 chips     │
│ [全部44] [身份] [票据]... │
├─────────────────────────┤
│ 接口档案                  │
│ ┌缩略图┐  二代证           │
│ │     │  字段/摘要/详情    │
│ └─────┘                  │
│ ┌缩略图┐  增值税发票       │
├─────────────────────────┤
│ 样张/完整目录作为后置辅助   │
└─────────────────────────┘
```

详情页手机端：

```text
┌─────────────────────────┐
│ OCR 识别接口文档中心      │
│ 首页  全部接口  开通流程   │
├─────────────────────────┤
│ ← 返回    个人身份类       │
│ 二代证（人像页+国徽页）    │
│ 摘要说明，最多 2-3 行      │
├─────────────────────────┤
│ 测试环境                  │
│ [复制]  https://...       │
│ 生产环境                  │
│ [复制]  https://...       │
├─────────────────────────┤
│ [请求参数] [返回参数]      │
│ [错误码]   [示例]          │
├─────────────────────────┤
│ 请求参数                  │
│ Header / Body / 字段卡     │
├─────────────────────────┤
│ 返回参数                  │
│ 统一结构 / 业务字段卡       │
└─────────────────────────┘
```

开通流程页手机端：

```text
┌─────────────────────────┐
│ OCR 识别接口文档中心      │
│ 首页  全部接口  开通流程   │
├─────────────────────────┤
│ 银商大脑 OCR 开通流程      │
│ 开通 OCR 需完成两步...     │
├─────────────────────────┤
│ 01 申请开放平台参数        │
│ 申请 APPID / APPKEY       │
│ 02 提交开发需求           │
│ 补齐 OCR 能力申请表        │
├─────────────────────────┤
│ 流程一 | UP 系统申请...    │
│ 01 登录 UP 系统            │
│ 02 搜索并进入流程          │
│ [步骤截图] [查看大图]      │
│ 03 填写字段               │
│ 04 核对提交               │
├─────────────────────────┤
│ 流程二 | 项目管理中心...   │
└─────────────────────────┘
```

### M7 分期建议

1. M7.1 PC 锁定与移动端覆盖层整理。
   - 清理中断前临时 `Mobile polish pass` 草稿样式。
   - 建立文件末尾的单一 `Mobile v2` 覆盖区。
   - 拍 PC `1600x900` 基线截图。

2. M7.2 首页手机端成品化。
   - 压缩共享顶部导航占高。
   - 把首页手机端主阅读顺序稳定为标题、搜索、类型筛选、接口卡。
   - 样张降为证明性视觉，不再拖慢进入接口卡。

3. M7.3 详情页手机端成品化。
   - 标题自然换行，不横向裁切。
   - 地址、复制按钮和请求/返回入口形成清晰接入起步区。
   - 元信息和 PDF 下载降为二级信息。

4. M7.4 开通流程页手机端成品化。
   - 顶部两条流程入口改为手机单列任务清单。
   - 步骤卡突出动作句，截图和说明降噪。

5. M7.5 回归与文档收口。
   - 验证 `390x844`、`428x932`、`1600x900`。
   - 更新本文件和 `SESSION_PROGRESS.md` 的最新状态。

## 进度维护规则

本文件是手机端专项的主进度指针。继续手机端任务时，以本节和“当前手机端专项状态”为准。

### 什么时候更新

每次手机端专项发生以下变化时，都要更新本文件：

- 完成一个阶段或阶段内的关键任务。
- 开始一个新阶段。
- 某个阶段被暂停、回溯或插队。
- 用户提出新的手机端规则、禁忌、验收口径或优先级变化。
- 发现影响后续判断的重要问题，例如新横向溢出、桌面回退、素材性能风险。
- 做完一次有价值的验证，例如 390px/430px 截图、`scrollWidth` 检查或 build。

不需要因为每个很小的 CSS 微调都更新本文件。微调只在形成结论、完成子任务或改变下一步时记录。

### 怎么知道现在到哪一步

看两个位置：

1. 文档顶部的“当前手机端专项状态”。
   - 这里是最快的状态表，用来判断当前主线阶段。
   - `下一步` 表示新 session 应优先进入的阶段。
   - `进行中` 表示当前正在做但尚未完成。
   - `暂停` 表示已经开始但因为插队、依赖或用户转向暂时不继续。
   - `回溯中` 表示之前阶段需要返工。
   - `已完成` 表示该阶段已经达到本文对应验收。

2. 文档底部的“推进日志”。
   - 这里记录为什么切换阶段、为什么插队、临时任务做了什么、下一步恢复到哪里。
   - 如果状态表和日志看起来冲突，以最新日期的推进日志为准，然后先修正文档状态表。

### 插队、回溯和临时任务怎么处理

手机端专项允许不严格按 M0 到 M6 顺序推进，但必须显式记录。

如果提前进入后续阶段：

- 把原阶段标为 `暂停` 或保留 `进行中`，并写清楚暂停原因。
- 把新阶段标为 `提前插队 / 进行中`。
- 在推进日志记录：为什么插队、插队范围、完成后回到哪个阶段。

如果回溯到已完成或较早阶段：

- 把被回溯阶段标为 `回溯中`。
- 写清楚触发原因，例如新发现破版、用户否定、桌面回退、验收失败。
- 回溯完成后重新写一次验证结果，再恢复原主线。

如果临时做一个不属于 M1 到 M6 的任务：

- 在推进日志新增 `TEMP-日期-简短标题`。
- 写清楚它是否阻塞主线。
- 如果不阻塞，完成后回到原阶段。
- 如果阻塞，把原阶段标为 `暂停`，直到临时任务完成。

推荐记录格式：

```text
### YYYY-MM-DD

- 当前主线：M1 接口详情页手机端可用性修复。
- 本次变化：临时插队到 M3 首页手机端搜索区，因为用户优先要求首页搜索可用。
- 处理范围：只调整搜索区，不处理完整首页卡片列表。
- 验证结果：390px 无横向溢出，桌面 1600x900 未回退。
- 下一步：回到 M1.2 详情页阅读顺序重排。
```

## 工作原则

1. 桌面端当前结构不作为手机端任务的主动改版对象。手机端改动要尽量限制在移动断点、共享组件的必要状态，以及不会破坏桌面的结构修正。
2. 手机端目标不是“保留桌面所有视觉元素”，而是让用户更快完成搜索、筛选、复制接口地址、查看请求参数、查看返回参数、理解字段来源。
3. 页面本身禁止横向滚动。只允许 URL、代码块和确有必要的表格区域在自身容器内横向滚动。
4. 档案材质要保留，但手机端需要降低装饰成本：少层纸面、少大阴影、少无任务意义的大留白。
5. 桌面 hover 逻辑在手机端必须改为 tap 逻辑。字段来源、卡片状态、锚点激活都要适合触控。
6. 验收优先覆盖 `390x844`、`430x932` 和桌面 `1600x900`。移动端任务不以 1280 默认截图作为最终视觉依据。

## 验收总标准

每个手机端阶段完成后至少验证：

- `document.documentElement.scrollWidth <= document.documentElement.clientWidth`。
- 390px 下没有正文、标题、按钮、搜索框、URL、代码块把页面撑宽。
- 390px 下触控目标高度原则上不低于 44px，高频操作按钮清楚可点。
- 首页首屏能完成搜索，且不需要滚过完整索引目录才看到接口入口。
- 详情页首屏或接近首屏能看到接口标题、测试/生产地址入口、请求参数和返回参数入口。
- 开通流程页首屏能理解两条主流程，不被重复索引或无关卡片干扰。
- 桌面 `1600x900` 截图不发生明显结构回退。
- `PATH="$PWD/.tools/node/bin:$PATH" npm run build` 通过。

## M0 现状诊断与规划

状态：已完成。

已完成内容：

- 读取 `PROJECT_VISION.md`、`DESIGN.md`、`SESSION_PROGRESS.md`。
- 使用 live 页面截图对比桌面与手机端。
- 确认详情页是当前手机端最大风险，首页和流程页属于效率优化。
- 建立本文件作为手机端专项的规划与进度指针。

诊断截图曾用于本轮分析，现已清理，不作为长期视觉来源。

## M1 接口详情页手机端可用性修复

目标：先把详情页从“横向破版”修成“手机可读、可复制、可跳转”的核心任务页面。

优先级：P0。

子任务状态：

- M1.1 修复详情页横向溢出：已完成。
- M1.2 重排手机端详情页阅读顺序：下一步。
- M1.3 改造接口地址区域：部分基础样式已在 M1.1 收口，仍需结合 M1.2 继续优化。
- M1.4 调整快捷入口：待开始。
- M1.5 初步收口请求参数和返回参数区域：基础 overflow 已处理，字段卡片化留到 M4。

建议拆分：

1. 修复详情页横向溢出。
   - 给 `.doc-content` 直接子级、`.detail-hero`、`.section-block`、代码块、表格包裹层补齐必要的 `min-width: 0`。
   - 检查 `.endpoint-row code`、`.field-table-wrap table`、`.request-skeleton code`、`.example-card code` 是否只在自身容器内滚动。
   - 验证 390px 下 `scrollWidth` 不再超过 `clientWidth`。

2. 重排手机端详情页阅读顺序。
   - 桌面左侧 `side-nav` 在手机端不应占据正文前的主要阅读空间。
   - 手机端建议顺序：返回按钮、分类、接口标题、摘要、测试/生产地址、请求/返回/错误码快捷入口、正文内容。
   - `side-nav` 在手机端保留，但移动目录块放到正文后方，避免占据首屏主阅读路径。

3. 改造接口地址区域。
   - 手机端每个环境地址变成纵向卡片：环境名、复制按钮、URL。
   - URL 使用单行内部横滚或可换行的等宽文本，不能撑开整页。
   - 复制按钮保持固定尺寸，避免长 URL 挤压按钮。

4. 调整快捷入口。
   - `请求参数`、`返回参数`、`错误码`、`请求 / 返回示例` 在手机端使用横向可滑标签条，减少首屏占高。
   - 点击后滚动位置要避开吸顶导航遮挡。

5. 初步收口请求参数和返回参数区域。
   - 表格可暂时保留内部横滚，但外层不能撑宽。
   - 更理想的后续方案是字段卡片化，放到 M4。

验收：

- `/interfaces/idcard` 和 `/interfaces/vat-invoice` 在 390px 下无页面横向滚动。
- 详情页第一屏不再只显示目录卡，至少能看到接口标题。
- 接口地址复制按钮在 390px 下完整可见。
- 桌面 `1600x900` 详情页左侧导航和首屏接入主线不退化。

## M2 全站手机端导航与纸面密度

目标：建立一套统一的手机端顶部导航和纸面密度规则，供首页、详情页、开通流程页复用。

优先级：P1，建议在 M1 之后开始。

建议拆分：

1. 压缩统一 `SiteHeader`。
   - 手机端保留品牌识别，但不再用桌面式大纸条承载完整三段布局。
   - 支持按钮改为紧凑按钮或次级纸质按钮，不应占满首屏过多高度。
   - 主导航可以保留横向滚动，但要有清楚的当前态。

2. 建立手机端纸面规则。
   - 外层蓝色档案板保留，但左右 padding 减少。
   - 纸面容器减少嵌套，避免“纸卡套纸卡套纸卡”。
   - 装订环、夹子、大章类装饰在手机端默认隐藏或缩小。

3. 统一移动端间距。
   - 首屏模块之间的蓝色空隙要服务结构，不要形成无内容空白。
   - 大标题字号不使用 viewport 缩放，按固定断点控制。

验收：

- 首页、详情页、流程页的手机导航同构。
- 390px 下导航区域不应超过首屏高度的约 25%。
- 支持入口可见但不压过页面主任务。

## M3 首页手机端结构重排

目标：让手机用户先搜索和进入接口，而不是先滚完桌面索引。

优先级：P1。

建议拆分：

1. 首屏结构重排。
   - 保留 `OCR 接口图鉴`、一句短描述、搜索框。
   - 右侧样张陈列不要照搬桌面四卡。手机端可以只保留一张代表性样张，或做横向小型样张带。
   - 搜索 placeholder 缩短，避免在 390px 下被截断。

2. 分类筛选改成横向标签条。
   - 现在完整“索引目录”占用过多空间，应改为手机端分类 chips。
   - 完整索引目录可以折叠在后面，或移动到页面底部。

3. 接口卡列表前置。
   - 用户在首页滚动较短距离就应该看到第一张接口卡。
   - 手机端接口卡应更像纵向档案条目：缩略图、分类、标题、字段摘要、查看详情。
   - 卡片不应因为纸面边框和外层 padding 变成窄小内容区。

4. 完整目录降级。
   - `完整接口目录` 在手机端保留，但不应早于主卡片列表。
   - 可考虑折叠、分组列表或页面底部索引。

验收：

- 390px 首页首屏能看到标题和搜索框。
- 搜索框文本、图标、输入内容不被截断。
- 390px 下第一张接口卡应明显早于当前约 1277px 的位置出现。
- 分类切换、搜索过滤、进入详情均可用。

## M4 字段表与代码块移动交互

目标：把桌面端宽表格和代码示例翻译成手机端更易扫读、更易复制的阅读块，同时不回退桌面结构。

优先级：P1 到 P2，建议在 M1 的基础可用性修复之后推进。

建议拆分：

1. 字段表卡片化。
   - 桌面继续用表格。
   - 手机端把每行字段转成字段卡：中文名、key、类型、标签、说明。
   - 标签和 key 要可扫读，不要堆成过密 chip。

2. 请求 / 返回示例代码块优化。
   - 保持代码结构，不强行换行破坏 JSON。
   - 代码块内部横滚，外层卡片不撑宽。
   - 复制按钮位置固定清楚。

验收：

- 字段表不再要求用户横向拖动整张宽表才能理解字段。
- Header / Body / 请求示例 / 返回示例 都有清楚可点的复制入口。
- JSON 示例不撑破页面，且复制按钮不会挤坏标题区。

说明：

- 原规划中的“字段来源 tap 模型”已失效，因为详情页字段来源模块已在产品方向上正式下线。
- 后续不要把本阶段重新理解成恢复字段来源样张；本阶段只负责字段表和代码块的移动端交互。

## M5 开通流程页手机端优化

目标：保持“流程优先”，让手机端更像清晰的任务清单。

优先级：P2。

建议拆分：

1. 首屏压缩。
   - 标题、说明、两条主流程入口保留。
   - 避免顶部导航和大标题共同占用过多首屏。

2. 弱化重复索引。
   - 手机端已有两条流程入口时，下面的“流程索引”可以折叠或简化。
   - 不要把附件说明、截图说明伪装成并列主流程。

3. 步骤卡优化。
   - 步骤编号、标题、说明、截图入口形成稳定结构。
   - 有截图的步骤应明确“查看大图”按钮。

4. 截图 lightbox 手机化。
   - 手机端使用近全屏预览。
   - 关闭按钮、标题、焦点说明可见。
   - 大图应适合纵向和横向截图，不产生整页横向滚动。

验收：

- 390px 下无横向滚动。
- 首屏可理解两条主流程。
- 点击截图可打开、关闭，说明文字不遮挡图片。

## M6 验证、回归与文档收口

目标：把手机端专项变成可维护状态，而不是一次性补丁。

建议拆分：

1. 建立手机端截图清单。
   - 首页：390、430、1600。
   - 详情页：`idcard`、`vat-invoice` 的 390、430、1600。
   - 开通流程页：390、430、1600。

2. 建立布局指标检查。
   - 每页检查 `scrollWidth <= clientWidth`。
   - 记录异常元素选择器。

3. Build 与交互回归。
   - `npm run build`。
   - 搜索、筛选、详情跳转、锚点跳转、复制按钮、流程截图 lightbox。

4. 更新文档。
   - 每完成一个阶段，更新本文件的“当前状态”和对应阶段状态。
   - 在 `SESSION_PROGRESS.md` 中记录最新完成阶段和下一步。

## 推进日志

### 2026-06-03

- 启动手机端专项规划。
- 当前不改 UI 代码，先建立任务规划和进度跟踪。
- 下一步建议直接进入 M1：接口详情页手机端可用性修复。

### 2026-06-03 M1.1 开始

- 当前主线：M1 接口详情页手机端可用性修复。
- 本次变化：开始 M1.1，优先修复 `/interfaces/idcard` 和 `/interfaces/vat-invoice` 在 390px 下的页面横向溢出。
- 处理范围：只处理详情页移动端 overflow containment、长 URL、表格和代码块外层撑宽问题；不重排首页和开通流程页。
- 下一步：完成 CSS 修复后验证 390px/430px 指标，并做桌面 `1600x900` 烟测。

### 2026-06-03 M1.1 完成

- 当前主线：M1 接口详情页手机端可用性修复。
- 本次完成：详情页移动端 overflow containment。`/interfaces/idcard` 和 `/interfaces/vat-invoice` 在 390px 下不再发生页面级横向滚动。
- 关键 CSS 范围：`src/styles.css` 中 720px 以下详情页规则，重点约束 `.detail-board`、`.doc-content > *`、`.detail-hero`、`.section-block`、`.endpoint-row`、`.field-table-wrap`、代码块和示例卡片。
- 验证结果：`idcard-390`、`vat-390`、`idcard-430`、`idcard-desktop-1600` 均满足 `scrollWidth === clientWidth`。表格仍保留内部横向滚动，符合 M1.1 目标。
- 构建结果：`PATH="$PWD/.tools/node/bin:$PATH" npm run build` 通过。
- 桌面与手机端截图曾用于对应指标验证，截图文件现已清理。
- 下一步：进入 M1.2，处理手机端详情页阅读顺序。目前目录仍排在正文前方，属于 M1.2 范围。

### 2026-06-04 M1.2 完成

- 当前主线：M1 接口详情页手机端可用性修复。
- 本次完成：手机端详情页阅读顺序重排。详情页现在在移动端优先呈现返回入口、分类、接口标题、摘要、测试/生产地址和快捷入口，桌面端左侧目录保持不变。
- 处理范围：新增移动端顶部返回栏、正文前快捷入口条、正文后页面目录块；移动端隐藏 hero 样张与字段条，避免它们继续抢占首屏高度。
- 验证结果：`PATH="$PWD/.tools/node/bin:$PATH" npm run build` 通过。
- 下一步：进入 `M1.3 改造接口地址区域`。

### 2026-06-04 M1.3 完成

- 当前主线：M1 接口详情页手机端可用性修复。
- 本次完成：手机端接口地址区域改造。测试 / 生产地址改成纵向卡片式排列，复制按钮前置，URL 退到下方单独显示，避免长地址和按钮在同一横排竞争空间。
- 处理范围：只改详情页移动端地址区和相关样式，桌面端三列结构保持不变。
- 验证结果：`PATH="$PWD/.tools/node/bin:$PATH" npm run build` 通过。
- 下一步：进入 `M1.4 调整快捷入口`。

### 2026-06-04 M1.4 完成

- 当前主线：M1 接口详情页手机端可用性修复。
- 本次完成：手机端快捷入口改成横向可滑标签条，并加入轻量编号，保留 `请求参数`、`返回参数`、`错误码`、`请求 / 返回示例` 的直达能力，同时减少首屏占高。
- 处理范围：只改详情页移动端快捷入口条和配套样式，不改正文结构。
- 验证结果：`PATH="$PWD/.tools/node/bin:$PATH" npm run build` 通过。
- 下一步：进入 `M1.5 初步收口请求参数和返回参数区域`。

### 2026-06-04 M1.5 完成

- 当前主线：M1 接口详情页手机端可用性修复。
- 本次完成：请求 / 返回 / 错误码 / 示例四块在手机端做了初步收口，压缩了标题说明、索引块、示例卡片和代码块的占高，避免它们继续把正文主线顶得太靠下。
- 处理范围：只改详情页移动端请求、返回、错误码与示例区域的密度，不改桌面和正文结构。
- 验证结果：`PATH="$PWD/.tools/node/bin:$PATH" npm run build` 通过。
- 下一步：进入 `M2 全站手机端导航与纸面密度`。

### 2026-06-04 M2 批量收口完成

- 当前主线：M2 全站手机端导航与纸面密度。
- 本次完成：把首页与开通流程页的移动端头部、导航间距、首屏纸面边距、样张占高、目录块和流程索引整体收紧，统一成更克制的手机端节奏。
- 处理范围：只改首页和开通流程页的移动端共性规则，不动桌面结构，也不提前进入首页内容结构重排。
- 验证结果：`PATH="$PWD/.tools/node/bin:$PATH" npm run build` 通过。
- 下一步：等待 390px / 430px 浏览器视觉验收，验收后进入 `M3 首页手机端结构重排`。

### 2026-06-04 M3 开始

- 当前主线：M3 首页手机端结构重排。
- 本次变化：开始把首页从桌面目录先行改成手机端搜索优先，并准备压缩首页首屏、分类区和目录区的阅读顺序。
- 处理范围：只处理首页手机端结构，不动详情页和开通流程页正文。
- 下一步：完成首页首屏、分类筛选、接口卡前置和完整目录后置的重排。

### 2026-06-04 M3 结构收口

- 当前主线：M3 首页手机端结构重排。
- 本次变化：把首页移动端最终覆盖层追加到样式文件末尾，直接压缩 hero、把分类改成横向 chips、让接口卡列表前置，并把完整目录后置。
- 处理范围：只处理 `max-width: 720px` 下的首页布局，不改桌面结构，也不扩展到开通流程页正文。
- 验证结果：`390px` 和 `430px` 下 `scrollWidth === clientWidth`，首屏保留一张代表性样张，接口卡前置，完整目录明显后置。
- 下一步：进入 `M4 字段表 / 代码块移动交互`。

### 2026-06-05 M4 完成

- 当前主线：M4 字段表与代码块移动交互。
- 本次完成：详情页字段表在手机端切换成字段卡视图，桌面端继续保留原表格；`Header`、`Body`、`请求示例`、`返回示例` 四个代码块统一补齐复制按钮。
- 处理范围：只改详情页 `FieldTable`、`RequestSkeleton`、示例代码卡和移动端样式，不恢复字段来源模块，也不改首页与流程页结构。
- 验证结果：`PATH="$PWD/.tools/node/bin:$PATH" npm run build` 通过。
- 下一步：进入 `M5 开通流程页手机端优化`。

### 2026-06-05 M5 完成

- 当前主线：M5 开通流程页手机端优化。
- 本次完成：手机端首屏保留两条主流程入口，但把卡片标题改成短任务名；重复的流程索引降成正文后方折叠块；步骤卡重新排成“动作 / 说明 / 截图入口 / 附件”单列；截图 lightbox 改成手机近全屏。
- 处理范围：改动 `ActivationGuidePage` 的流程索引 DOM、顶部流程入口文案、截图弹层滚动锁定和 `max-width: 720px` 下的流程页最终覆盖样式；桌面端流程结构保持不变。
- 验证结果：
  - `PATH="$PWD/.tools/node/bin:$PATH" npm run build` 通过。
  - Playwright 实测 `390px` 与 `428px` 下 `scrollWidth === clientWidth`。
  - 两个移动视口下都能看到 `2` 条主流程入口，折叠索引可展开，截图 lightbox 可打开并用 `Esc` 关闭。
  - 桌面 `1600x900` 下 `scrollWidth === clientWidth`，桌面索引保持显示，移动索引保持隐藏。
- 桌面与手机端截图曾用于回归验证，截图文件现已清理。
- 下一步：进入 `M6 验证、回归与文档收口`。

### 2026-06-05 M6 完成

- 当前主线：M6 验证、回归与文档收口。
- 本次完成：补了一轮 Playwright 全站回归，并顺手修掉两处回退。
- 回退一：首页手机端最后一层覆盖把分类 chips 又打回成了竖排列表，导致第一张接口卡重新被压到过低位置；现已恢复为横向可滑 chips，`390px` 下首张接口卡顶部回到约 `1032px`，重新满足早于旧基线 `1277px` 的要求。
- 回退二：从首页点击接口卡进入详情页时，仍会短暂继承旧滚动位置；现已改为在进入新 pathname 的入口上先做瞬时置顶，并配合 `RouteScrollManager` 的滚动记录收口，让详情页进入时直接落在顶部。
- 回归结果：
  - Playwright 实测首页、`/interfaces/idcard`、`/activation-guide` 在 `390px`、`428px`、`1600x900` 下都满足 `scrollWidth === clientWidth`。
  - 首页手机端搜索 `发票` 时结果缩到 `4` 项，首项为 `增值税发票`；分类条保持 `display: flex` 且 `overflow-x: auto`。
  - 详情页手机端首屏保留 `2` 个环境地址、`4` 个快捷入口，字段表为字段卡，代码块复制按钮为 `4` 个。
  - 流程页手机端保留 `2` 条主流程入口，折叠索引可展开，截图 lightbox 可打开并关闭，打开时 `body` 滚动被锁定。
  - 桌面端详情页侧栏和表格、流程页桌面索引都保持原显示策略。
- 构建结果：`PATH="$PWD/.tools/node/bin:$PATH" npm run build` 通过。
- 回归截图曾用于验证首页、详情页和流程页，截图文件现已清理。
- 下一步：手机端专项已完成当前计划；后续如继续，默认转入用户新的专项或视觉回合，而不是继续扩写本计划。

### 2026-06-05 M7 启动

- 当前主线：M7 手机端成品化体验优化。
- 本次决策：用户确认 M6 的“无横向溢出 / 可用”不等于手机端成品，后续手机端要按任务优先重新收口。
- 第一性判断：
  - 首页用户最先要搜索、筛选并进入接口，不是先完整欣赏桌面档案陈列。
  - 详情页用户最先要判断接口是否匹配、复制测试 / 生产地址、进入请求参数和返回参数。
  - 流程页用户最先要判断走哪条流程，以及第一步做什么。
- PC 锁定规则：PC 端当前视觉视为冻结基线；手机端改动默认只允许进入移动端覆盖层，每期必须用 `1600x900` 桌面截图回归。
- 下一步：执行 M7.1，先清理中断前临时 `Mobile polish pass` 草稿样式，建立文件末尾单一 `Mobile v2` 覆盖区，然后按首页、详情页、流程页顺序推进。

### 2026-06-05 M7 首轮完成

- 当前主线：M7 手机端成品化体验优化首轮。
- 本次完成：
  - 已清理中断前误置于文件顶部的临时 `Mobile polish pass` 草稿样式。
  - 已在 `src/styles.css` 文件末尾建立 `M7 mobile v2` 覆盖区，避免和 PC 端规则混在一起。
  - 首页手机端隐藏首屏样张陈列，把样张挪为接口卡之后的移动端辅助样张；接口卡改为上图下文的单列卡片，让标题、摘要、字段和详情入口稳定可读。
  - 详情页手机端把快捷入口改为 2x2 任务按钮，标题允许自然换行，地址和复制按钮保持首屏可用。
  - 开通流程页手机端把顶部两条流程入口改为单列任务清单，步骤卡继续保持单列。
- 回归结果：
  - `PATH="$PWD/.tools/node/bin:$PATH" npm run build` 通过。
  - Playwright 实测首页、`/interfaces/idcard`、`/interfaces/vat-invoice`、`/activation-guide` 在 `390x844`、`428x932`、`1600x900` 下均满足 `scrollWidth === clientWidth`。
  - 首页首张接口卡在 `390px` 下顶部约为 `617px`，已经进入首屏内，明显早于 M6 的约 `1032px`。
  - 桌面 `1600x900` 复拍首页、详情页和流程页，未发现 PC 端顶部导航、详情页侧栏、流程页桌面索引回退。
- 桌面与手机端截图曾用于 M7 首轮回归，截图文件现已清理。
- 下一步：等待用户对 M7 首轮手机端视觉验收；若继续优化，优先微调首页接口卡图片裁切、顶部支持入口可见性和流程页步骤密度。
