# OCRWeb Product Vision

> 从 `设计思路.docx` 中提炼迁移而来。原文是一段早期人与 Agent 的设计讨论；本文保留产品判断和设计原则，去掉对话式表达。

## 定位

OCRWeb 不应该只是一个接口参数查询站，而应该是一个 **OCR 能力中心**：帮助用户理解这个 OCR 体系能识别什么、适合什么业务场景、返回什么结构化结果，以及如何接入。

它面向的不只是开发人员，也包括产品、测试、运营、业务和内部推广场景。页面应该让人先看懂能力，再进入接口细节。

核心体验链路：

```text
原始材料图片 -> OCR 字段框选 -> 结构化结果 -> 接口调用方式 -> 接入建议
```

## 北极星

用户进入网站后，第一眼看到的应该是“能力体系”，而不是孤立接口列表。网站要像一个可浏览、可搜索、可理解的 OCR 能力图鉴，让用户知道：

- 我有什么材料要识别。
- 应该使用哪个 OCR 接口。
- 这个接口会提取哪些字段。
- 字段来自图片上的哪个位置。
- 返回结果如何被业务或系统使用。
- 接入时有哪些注意事项。

## 产品原则

1. **能力体系优先于接口堆叠**
   首页和导航应按证照、票据、车辆、支付、通用文档等能力组组织，而不是只罗列接口名称。

2. **场景优先于技术名词**
   支持按商户入网、财务报销、车辆管理、发票审核、支付核验、风控稽核等业务场景理解接口组合。

3. **先展示识别效果，再展示接口参数**
   详情页第一屏应优先回答“这个接口能识别什么、效果是什么”，请求地址、Header、参数表和错误码应在用户建立直观理解后出现。

4. **让字段来源可见**
   OCR 文档最有价值的交互是图片区域和 JSON/字段说明之间的联动高亮。用户悬停图片字段时，对应返回字段高亮；悬停返回字段时，图片上的来源区域高亮。

5. **同一结果服务多类读者**
   返回结果不只提供 JSON，也应支持字段表格视图和图片标注视图。开发看 JSON，业务和测试看表格，所有人都能通过图片标注理解字段。

6. **文档应提供接入判断**
   除参数表外，接口详情还应提供适用场景、推荐图片质量、拍摄/上传建议、字段缺失处理、是否需要人工复核、适合前端直传还是后端转发等接入建议。

7. **数据结构化驱动页面**
   不应手工维护几十个重复页面。每类 OCR 能力应由结构化数据描述，再由统一模板渲染：首页卡片、详情页、字段表、相关接口和场景包都应来自同一数据源。

## 首页方向

首页应承担“能力导航”和“快速找到接口”的任务。

推荐模块：

- 产品识别区：明确这是 OCR 识别接口图鉴 / OCR 能力中心。
- 强搜索：支持按接口名、业务词、材料名、字段名搜索，例如“报销”“发票”“身份证”“车牌”“金额”。
- 能力分类：证照、票据、支付与商户经营、车辆与交通、通用文档等。
- 能力卡片矩阵：展示接口名称、分类、适用场景、字段摘要和入口。
- 场景化接口包：商户入网、财务报销、车辆业务、支付核验等组合。
- 热门/最近更新：让网站有持续维护的感觉。

## 详情页方向

每个接口详情页建议采用稳定结构：

- 接口概览：名称、分类、能力说明、典型场景。
- 识别效果演示：样例图片、字段标注、结构化结果。
- 请求说明：地址、方式、Header、请求参数、示例。
- 响应说明：返回结构、字段含义、是否必返、示例值。
- 错误与异常：错误码、原因、处理建议。
- 接入建议：图片质量、调用方式、字段兜底、人工复核建议。
- 相关接口：同场景或同材料链路下的推荐能力。

如果未来加入在线调试，应分清两种模式：

- 示例模式：使用内置样例图片和静态响应，不调用真实接口。
- 真实调试模式：用户上传图片并调用真实接口，需要额外处理鉴权、隐私、配额和错误反馈。

当前项目是前端图鉴站，默认不承诺真实上传、在线调试、API 调用、凭证存储或真实样本处理，除非后续任务明确要求。

## 已完成的最小版本基线

当前代码库已经完成最小可落地版本基线，至少包括这些能力：

- 能力卡片首页，按分类展示 OCR 能力。
- 统一接口详情页模板，覆盖说明、场景、请求、响应、示例和错误。
- 样例图片 + 返回字段的联动理解体验。
- 场景化接口包，帮助用户按业务流程找接口。
- 强搜索，支持接口名、业务场景和字段关键词。

这些能力已经足以让当前项目明显区别于普通接口文档站。后续默认重点不再是证明“能不能做成图鉴站”，而是保持现有产品方向稳定，并推进兼容性、清理和质量收口。

## 当前视觉关系

早期设想中提到过科技感、玻璃卡片、扫描线、蓝紫霓虹等方向。当前项目已经迭代成更明确的 **纸质档案 / 蓝色档案板 / OCR 接口图鉴** 风格。

因此：

- 产品目标以本文为准。
- 当前视觉系统以 `DESIGN.md` 为准。
- 做新页面或组件时，应把“能力中心”和“字段可视化”的产品原则，翻译到当前档案图鉴风格中，而不是回退到泛科技感后台或玻璃拟态风格。

## 不应回退到的形态

- 不要做成只有接口名和参数表的普通文档列表。
- 不要做成通用后台管理系统。
- 不要让视觉装饰压过 OCR 材料、字段和接口信息。
- 不要在没有真实后端能力时暗示已经支持真实上传或真实调试。
- 不要让历史截图、旧 Word 或旧交付图覆盖当前源码和 `DESIGN.md` 里的设计判断。
